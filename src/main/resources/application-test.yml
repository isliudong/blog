spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/blog?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: afs@gs45%
server:
  port: 9090
mybatis-plus:
  #mapper-locations: classpath:mapper/*.xml
  #type-aliases-package: com.ld.blog.mapper
  global-config:
    db-config:
      id-type: auto
  configuration:
    map-underscore-to-camel-case: true
blog:
  file-sever-path: /data/file-server
  port: 8211
